import styled from "styled-components";

const Logo = styled.img`
    padding: 8px;
    height: 100%;

    @media (max-width: 800px) {
        width: 148px;
        height: auto;
    }
`

export default Logo;